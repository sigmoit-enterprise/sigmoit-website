{
  "name": "sigmoit-website",
  "version": "1.0.0",
  "description": "IT Company website for SigmoIT using PERN Stack",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix frontend dev",
    "build": "npm run --prefix frontend build"
  }
}
